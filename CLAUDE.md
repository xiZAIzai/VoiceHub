# CLAUDE.md - Claude Code 工作规范

## 1 核心原则

你是 Claude Code，在本仓库内协助完成开发任务。首要目标是按计划稳定推进，保持改动可验证。

1. **文件驱动** — 决策写进 PLAN.md / TASKS.md，不依赖聊天记忆
2. **单任务聚焦** — 一次只做一件事，做完再下一件
3. **测试先行** — 先写测试定义预期，再写实现代码，保证结果的正确性
4. **功能解耦** — 每个模块独立可测，不耦合无关逻辑；单文件 ≤500 行，单函数 ≤50 行
5. **逐步验证** — 每次改动立即可运行、可检查，不攒大变更
6. **注释完善** — 文件、函数、核心逻辑必须有中文注释（docstring / 行内注释）
7. **文档同步** — 代码改完，立刻更新 TASKS.md（勾选任务、记录完成时间）和 PLAN.md（里程碑进度）
8. **最小改动** — 只改当前任务相关的文件和代码，不做额外重构
9. **类型安全** — 使用类型注解，避免 `Any` 滥用、隐式强转、非空断言
10. **诊断日志** — 打印出关键日志（状态流转、分发结果、耗时），方便排查问题

---

## 2 本项目开发原则

### 2.1 核心原则
1. 优先保证结构清晰，不要过度设计。
2. 按职责拆分（监听 / 路由 / 存储 / 广播），不按文件个数或页面拆分。
3. 保持单向数据流：热键事件 → 编排层更新状态 → WebSocket 广播 → 前端渲染。
4. 前端只负责展示与交互，不承载业务流程。
5. 优先简单、直接、稳定的方案。

### 2.2 分层职责
6. 入口（main）：进程启动、配置加载、组件组装。
7. Daemon 核心：全局热键、剪贴板监控、录音/转录状态机。
8. Router：目标设备路由与分发（本地粘贴 / LAN HTTP 推送）。
9. Storage：SQLite 读写（transcript_logs），不含业务流程编排。
10. Transport：WebSocket 广播 / HTTP 客户端 / 目标端接收服务。
11. Dashboard：Web 前端，订阅后端状态与历史记录。

### 2.3 状态管理
12. 一份真实状态只能有一个单一来源（核心 daemon 状态机），避免多处维护同一状态。
13. 状态变更入口要集中（状态机/事件总线），只能通过明确的方法修改状态。
14. 复杂流程（录音→转录→提取→分发）提取为可测试的纯逻辑编排层，不散落在回调里。

### 2.4 可靠性
15. 异步操作必须防竞态：录音 session、剪贴板轮询、分发请求都要带标识（sessionId/token）校验，过期丢弃。
16. 谁创建谁销毁。资源（热键、监听器、连接）生命周期与所有者绑定。
17. 副作用（网络请求、文件 IO、系统调用）通过接口/注入隔离，不在业务逻辑里直接散落。
18. 每个异步调用点都要考虑失败情况。沉默吞掉异常是 bug，应明确处理或向上传播。
19. 错误、加载、空状态必须显式设计，不要只写成功态。

### 2.5 可维护性
20. 命名优先于技巧，名称必须直接表达职责。
21. 目录结构按功能组织（daemon/router/storage/transport/web），再按模块分层。
22. 先允许少量重复，确认模式稳定后再抽象。过早抽象比重复更有害。
23. 优先测试状态流转和分发逻辑，不要只测 UI 表面。
24. 配置走 config.json 统一入口，不把魔数散落代码。

---

## 3 启动流程（每个会话强制执行）

开始任何工作前，必须按顺序完成以下 4 步：

1. 读取 PLAN.md — 了解项目当前阶段和整体规划
2. 读取 TASKS.md — 了解待办任务列表和优先级
3. 输出要执行的任务 — 明确说明接下来做哪一个任务（一次只做一个）
4. 等待用户确认 — 用户同意后再开始修改代码

---

## 4 收尾流程（每次完成任务强制执行）

完成当前任务后，必须按顺序完成以下 7 步：

### 步骤 1: 检查测试完整性
确认以下测试是否已覆盖：
- **Unit Test**: 纯逻辑测试（状态机、路由、存储、配置解析），不涉及 UI
- **Integration Test**: 端到端验证（热键→转录→分发链路；接收端收到文本并注入）

### 步骤 2: 删除死代码
检查是否存在未使用的代码（包括测试中的），有则删除。

### 步骤 3: 检查注释和文档
确保新增/修改的代码有清晰的中文注释。

### 步骤 4: 运行验证命令
```bash
python -m pytest
```

### 步骤 5: 更新 TASKS.md
```markdown
# 必须完成：
1. 勾选已完成任务（- [x]）
2. 在任务下添加完成记录：

  **完成时间**: 2026-08-19
```

### 步骤 6: 更新 PLAN.md（如有需要）
如果本次任务导致里程碑进度变化，必须更新 PLAN.md 中对应里程碑的状态。

### 步骤 7: 输出完成摘要
```markdown
**实现的任务**: [任务标题]
**修改的文件** (X 个):
- path/to/file.py (+50 -10)
**对应的测试**:
- path/to/test_file.py
**下一步建议**:
- 告诉用户如何验证结果
- 下一个任务是什么
```

---

## 5 TASKS.md 归档规则

满足以下任一条件时，必须执行归档：
1. **里程碑完成** — PLAN.md 中某个 Milestone 全部完成
2. **文件过大** — TASKS.md 超过 200 行
3. **任务过多** — 已完成任务超过 30 条
4. **手动触发** — 用户明确要求归档

归档步骤：
1. 创建归档文件：`docs/tasks-archive/milestone-X-completed.md`
2. 将已完成任务移入归档文件
3. 清理 TASKS.md，仅保留未完成任务
4. 在 TASKS.md 顶部添加归档链接
5. 更新 PLAN.md 里程碑状态

---

## 6 编码规范

### 6.1 Python 约定
- 类型注解：所有公开函数/方法标注参数与返回类型
- 测试框架：pytest，测试文件命名 `test_*.py`，放在 `tests/` 对应目录
- 每个新功能或 bug 修复必须包含对应测试
- 静态检查/格式化工具定稿后在 README 记录

### 6.2 前端约定
- Dashboard：HTML + Tailwind + Vue（单文件/CDN 引入，不引入重构建链，除非确有必要）
- 状态同步走 WebSocket，前端只读状态，不直接写后端

### 6.3 配置约定
- 所有可变项进 config.json（见 docs/PRD.md §6），代码读配置不写死
- 敏感信息（API Key）不进配置文件与 git，走环境变量

---

## 7 Troubleshooting（踩坑记录）

记录已修复的典型问题和设计约束，防止同类问题再次出现。随开发推进持续补充。

1. **ctypes 64 位句柄溢出**：Win32 句柄（HWND/HINSTANCE/HMODULE）是 64 位，但 ctypes 未声明函数签名时会默认按 32 位 int 转换，导致 `OverflowError: int too long to convert`。解决：为所有用到的 user32/kernel32 函数显式声明 `argtypes`/`restype`，句柄参数用 `wintypes.HANDLE/HWND/HINSTANCE/HMODULE`（均 c_void_p）。注意 `ctypes.wintypes` **没有 `LRESULT`**，用 `LRESULT == wintypes.LPARAM`（== LONG_PTR）代替。

2. **Win32 窗口线程亲和性（thread affinity）**：窗口在哪条线程创建，其消息（含 `PostMessage` 投递的 WM_CLIPBOARDUPDATE）就进哪条线程的队列；建窗线程和消息循环线程不一致会导致收不到异步消息（只收到建窗期间同步 SendMessage 的 WM_CREATE 等）。解决：把「注册类 + 建窗 + AddClipboardFormatListener + GetMessageW 消息循环」全部放进**同一条**线程函数，外部 stop 用跨线程 `PostMessage` 唤醒阻塞的 `GetMessageW`。
