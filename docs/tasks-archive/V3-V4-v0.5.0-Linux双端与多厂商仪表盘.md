# 归档：V3 Linux 双端 + V4 自建内核多厂商（至 v0.5.0，2026-08-31）

> 从 TASKS.md 归档的过程性记录。未完成事项仍在 TASKS.md 活页。
> 前置归档：[V1](./v1-completed.md) · [V2](./v2-completed.md) · [V4-M11-M12上-自建转写内核](./V4-M11-M12上-自建转写内核.md)

## V3：Linux 双端适配（2026-08-20 立项 → v0.3.0 收口 2026-08-25）

> 里程碑编号延续全局序列：M7 预研与选型 / M8 Linux 主控后端 / M9 接收端体验 / M10 工程化收尾。

- [x] 先行项（Ubuntu 22.04 基准，本机 WSL2 即刻开工）：搭 WSL2 Ubuntu 22.04 开发环境
  （python3.10 + xclip/xdotool + systemd）；CI 加 ubuntu-22.04 pytest job；
  输入栈 spike + linux_backend 可测代码按 X11 假设先行开发（TDD）。
  **完成时间**: 2026-08-20（WSL 环境由用户预装，助手完成其余全部）
  - CI：build.yml 加 linux job（ubuntu-22.04 + py3.10 + pytest），push 后生效；
    本地双平台实测 **Windows / WSL 各 104 全绿**（旧 94 + 新增 linux_backend 10 个）。
  - spike（`scripts/spike_linux_stack.sh`，openKylin 可复用）：xclip 中英 UTF-8 读写自洽 PASS、
    xdotool 注入 PASS；**WSLg 剪贴板桥与 Windows 不互通**（WSL 局限，不影响开发，真机无此问题）。
  - 开发（`voicehub/linux_backend.py` 159 行 + `tests/test_linux_backend.py` 10 用例）：
    xclip 版 read_text（显式 UTF-8 防无 LANG 环境乱码）、X11ClipboardPoller 轮询监听
    （仅武装期轮询；武装沿登记基线防假事件）、pynput 热键（不可用降级日志）、
    main.py 平台守卫扩展、requirements 加 `pynput; linux` 标记。
  - 端到端冒烟（`scripts/smoke_linux_e2e.sh`，openKylin 可复用）**全绿**：daemon 启动 →
    pynput 注册 3 热键 → xdotool 合成 Alt+2 武装 → xclip 写"转写" → 拦截路由 →
    接收端粘贴 → SQLite 落库（隔离端口 8100/5051/9998 + /tmp 隔离 db）。
  - 踩坑三条：① Git Bash → wsl.exe 有 MSYS 路径转换/引号双重坑，跨端命令一律
    `MSYS_NO_PATHCONV=1` + 脚本文件执行；② `xdotool key alt+2` 合成方式 pynput 收不到，
    必须 keydown/keyup 显式序列；③ WSL mirrored 网络 + Windows 真身 daemon 同跑会撞端口
    （8000/9898 shared），冒烟必须换独立端口。
- [x] M7：Linux 预研与选型。
  **完成时间**: 2026-08-20（ADR-7 定案：xclip 读写/轮询监听 + xdotool + pynput，WSL 真跑验证全绿）
  - 策略变更（用户决策）：**取消 openKylin 真机前置验证**——Ubuntu 22.04 构建的 AppImage
    历来在 openKylin 正常运行（自包含 + glibc 兼容），以 AppImage 交付、差异随用随修。
- [x] M8：linux_backend 主控后端 + 全链路验证（WSL 内完成）。
  **完成时间**: 2026-08-20（代码 + 双平台 104 测试 + WSL 端到端冒烟全绿）
- [x] openKylin 实机随用随修第一轮（v0.3.0 AppImage 首次真机使用暴露，2026-08-25）。
  **完成时间**: 2026-08-25（三问题全部修复，110 单测 + 本机重打包全链路实测全绿，ADR-8）
  - ① 托盘没出现：**不是故障**——Linux 版 M8 定案无托盘；但 openKylin（Wayland/kylin-wlcom）
    下程序完全不可见不可接受 → 补齐 `linux_tray.py`（jeepney 直写 StatusNotifierItem +
    DBusMenu，菜单「打开仪表盘/退出」，UKUI 面板实测 6 步 DBus 验证全绿；无 watcher/无总线/
    无 jeepney 均降级日志）。
  - ② 仪表盘起不来：8000 端口被 openKylin 自带 kytensor（Triton 内核）占用，
    uvicorn bind 失败 → 默认端口 8000→8765（config 默认值 + 种子 + README + 本机已播种 config）。
  - ③ 热键全灭：Release AppImage 缺 `pynput.keyboard._xorg` + `python-xlib`——pynput≥1.8
    import 期即连 X server，headless CI 上 `collect_submodules("pynput")` **静默返回空表**
    （WSL 本地构建因 WSLg 有 DISPLAY 而正常，故当时冒烟全绿、Release 翻车）→ spec 显式
    hiddenimports 写死模块名；CI 加 xvfb + AppImage 冒烟断言「已注册 Linux 热键」；
    smoke 脚本补 EXTRACT_AND_RUN 残留进程清杀。
  - 本机（openKylin）用 conda py3.12 重建 `~/.venvs/voicehub` 重打包：冒烟 7 项全绿（含热键
    武装→路由→落库），桌面真实运行验证托盘注册/仪表盘 8765/托盘「退出」干净收口。
    注：conda py3.14 + venv 跑 PyInstaller 会段错误，py3.12 原生 conda 环境正常。
- [x] M10：daemon/receiver 打包 **AppImage** + WSL 内 AppImage 冒烟 + CI ubuntu job 扩展打包上传 +
  发 v0.3.0 Release + 文档收口。
  **完成时间**: 2026-08-25（**Release v0.3.0 已发布**，四产物；main 与 tag 流水线均全绿）
  - 打包链（2026-08-20）：`packaging/build_linux.sh` 一键产出双 AppImage；
    `paths.py` 加 `VOICEHUB_HOME`（AppImage 只读挂载 → 便携目录），AppRun 播种
    config/检测 xclip、xdotool；`scripts/smoke_appimage.sh` 冒烟全绿；
    CI linux job 打包上传 + tag 时 AppImage 进 Release；README Linux 章节。
  - 提交拆分（4 commits）：feat 主控后端 / feat 打包链 / docs 归档 / chore .gitattributes
    （强制 .sh LF，防 autocrlf 破坏 WSL 执行）。
- 闪电说 openKylin 适配调查（2026-08-25 第一轮，**glibc 已解 / UI 渲染待官方**）：
  - ✅ glibc：闪电说 0.7.5 要求 GLIBC_2.39（Ubuntu 24.10+ 基线），本机 2.38。免容器方案已落地：
    `~/bin/shandianshuo-launcher`（侧载 Ubuntu libc6 2.43 自定义 loader）。容器对 UI 渲染问题无效。
  - ❌ UI 渲染（定性为闪电说 × kylin-wlcom 兼容 bug，已试尽外部手段）：X11 后端窗口在
    XWayland 映射但不可见；Wayland 后端 "No monitor available"、WebKit 0 帧渲染。已试软件渲染
    全套与沙箱排查。**待反馈闪电说官方（内测版），外部等待项。**
  - 附带情报：闪电说 Linux 触发键为 **RAlt**；Tauri 应用（GTK 后端），非 Electron。

## V4：自建转写内核·云端 ASR（M11 全部完成 + M12 大半，2026-08-27 收口；延伸至 v0.5.0）

> 定案（ADR-9）：ASR 统一走云 / 文本直通 orchestrator 不经剪贴板链路 /
> 双引擎 config.engine 开关共存 / 触发键独立。M11 过程长文：
> [V4-M11-M12上-自建转写内核](./V4-M11-M12上-自建转写内核.md)。

- [x] **M11：最小闭环竖切** ✅ 2026-08-26（⑧项全勾）：provider spike → config transcription 段
  → dictation 包（vad/recorder[arecord 回退]/asr_client[WS v3 二进制协议，双头鉴权实测打通]/
  engine 会话令牌状态机）→ route_direct 直通路由 → 触发三通道 → 三重验收。
- [x] **M12-① 润色四模式** ✅ 2026-08-26：off/light/structured/custom + DeepSeek 客户端；
  失败降级原文铁律；raw_text 落专用列；关 thinking + ASR 大块快发提速（12s→3s 级）。
- [x] **体验定稿轮** ✅ 2026-08-27（用户验收「最终版本」）：AGC 波形悬浮框终态 / 键帽真实热键 /
  可拖动 / 持久线程架构零崩溃 / RECOGNIZING 扫光；wl-copy 双写根治 kylin-wlcom 剪贴板；
  快速触发脚本（Alt+9 响应 2~4s→65ms）；auto_paste 用户定案暂缓（机制保留）。
- [x] **双引擎改进轮** ✅ 2026-08-27：设置页「转写引擎」下拉（ConfigService 链路）；
  直通写剪贴板防抖守卫（rebase_baseline_if_armed，幂等）。
- [x] **README 双引擎章节** ✅ 2026-08-27。
- [x] **v0.4.0 Release** ✅ 2026-08-27（CI 全绿四产物；平台测试三处跨平台问题当场修——
  触发脚本 UTF-8+LF 强制 / 双写 which 桩）。
- [x] **v0.4.1：凭证自助填写通道** ✅ 2026-08-31：CredentialsService + 设置页「API 凭证」卡
  （脱敏 tail-4，key 永不出服务）；__version__ 归位并在仪表盘页脚展示；
  CI Release 加 generate_release_notes。安全核验四路证据：key 从未进 git 历史。

## 多厂商开放轮 + 仪表盘改版（2026-08-31，用户定案：一次做全不分二期）

- [x] **ASR 多厂商适配器**：`AsrProvider` 协议下新增 `OpenAICompatAsrClient`
  （POST /audio/transcriptions，覆盖硅基流动 SenseVoice / Groq / OpenAI / 本地 Whisper；
  pcm_to_wav 内存转码不落盘，httpx trust_env=False 对齐润色）；`build_asr_provider` 工厂
  （volcengine_sauc / openai_compat，wss 端点忘改回落硅基流动防呆）；TranscriptionConfig 增 model。
- [x] **润色多厂商预设**：UI 预设下拉（DeepSeek/Kimi/智谱/通义/火山方舟/OpenAI/自定义，
  选厂带出端点+默认模型可手改，老配置按 base_url 反查回显）。
- [x] **高级设置卡**（默认收起）：仪表盘端口、采样率、火山端点/资源 ID。
- [x] **仪表盘三主题美化**：深/浅/护眼（CSS 语义变量 + localStorage + data-theme）、
  语义化配色、卡片图标、Logo 渐变、悬浮保存栏；引擎卡前置，润色/凭证卡随引擎联动显隐
  （隐藏不删配置）。
- [x] **仪表盘秒开**：公网 CDN 直连 ~8s 白屏定案 → Tailwind + Vue 3.5.42 vendor 本地化
  （入 git + 双平台 spec datas 进包体 + 白名单静态路由）；打包实例 0.4ms/1ms，外网引用清零。
- [x] **四则体验修复**：select 弹出列表跟随主题；转写 API Key「旧版双头已就绪可不填」状态；
  去抖/粘滞参数精炼阐述；build_linux.sh type2 runtime 缓存 + --runtime-file（弱网抖动修复）。
- 测试：213 passed（OpenAI 兼容客户端 6 项 + provider 工厂 4 项 + vendor 路由 1 项新增）。

## 官网与文档（2026-08-31）

- [x] **website/index.html 科技风官网**：hero（语音控制台叙事）+ 特性六卡（多端分发置首标核心创新）+
  四步工作流 + 双引擎（含闪电说正式致谢 shandianshuo.cn）+ 下载卡（latest 直链）+ 隐私区 +
  入场动画；README 挂官网链接与致谢章节。
- [x] **website/docs.html 使用文档**：十章（快速上手/核心概念/引擎与供应商/润色厂商表含 Key 申请入口/
  多端分发/快捷键/VAD/本地接口参考[仪表盘 8 端点 + 接收端 /paste + UDP 9898]/数据隐私/FAQ）。
- [x] 叙事定稿：主控机/接收端（员工）协作模型，「一个人调度一支团队」，全站零具体设备假设、零 emoji
  （全 SVG 图标，系统缺字形不翻车）。

## v0.5.0 发版（2026-08-31 深夜）

- [x] 发版前「点击即用」修正：新装默认引擎 shandianshuo → builtin（未装闪电说的新用户
  不再静默无反应）；官网自动分流表述改为如实的「设置页指定谁就用谁」（未实现不虚假宣传）。
- [x] __version__ 0.5.0；tag v0.5.0 → CI 四产物 + bot 自动 Release（generate_release_notes）。
