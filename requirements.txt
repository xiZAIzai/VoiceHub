# VoiceHub 依赖
# 纯逻辑与 Web 部分跨平台；Windows 专属依赖用 sys_platform 标记，避免污染非 Windows 环境
fastapi>=0.110
uvicorn[standard]>=0.29

# Windows 专属：全局热键 / 剪贴板 / 托盘
keyboard>=0.13.5; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
pystray>=0.19; sys_platform == "win32"
Pillow>=10.0; sys_platform == "win32"

# 开发 / 测试
pytest>=8.0
httpx>=0.27  # FastAPI TestClient 依赖
